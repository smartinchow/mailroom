# All values are server-side only. Nothing here is ever exposed to the browser.
API_URL=http://localhost:3000            # mailroom-api base URL (no trailing /v1)
ADMIN_API_TOKEN=change-me                # must match the API's admin token
WEB_SESSION_SECRET=change-me-64-random-chars
ADMIN_PASSWORD=change-me
