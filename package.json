{
  "name": "mailroom",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted transactional email service fronting a cloud carrier (ACS / SES / SMTP)",
  "license": "AGPL-3.0-only",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:api": "pnpm --filter @mailroom/api dev",
    "dev:web": "pnpm --filter @mailroom/web dev"
  },
  "packageManager": "pnpm@10.33.0"
}
